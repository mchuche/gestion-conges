# Sert le build Vite à la racine du site (SPA) pour le conteneur "web".
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
