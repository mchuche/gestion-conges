# Archives SQL (schémas et scripts hérités)

Dossier **non utilisé** par l’application actuelle (**NestJS + Prisma** dans `api/prisma/`). Conservé au cas où tu récupèrerais d’anciennes définitions SQL ou comparerais un modèle passé.

## Contenu

- **`sql/`** — script « fresh install » historique
- **`migrations/`** — scripts ponctuels d’évolution
- **`ops/`** — réplication / perf (contexte ancien hébergement)
- **`legacy/`** — exports de schéma

Installation et évolution du schéma aujourd’hui : **`docs/guides/API_LOCAL_SETUP.md`** et les migrations Prisma.
